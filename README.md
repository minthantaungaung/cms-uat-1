# aia-myanmar-backend

## Latest Branch
- `UAT` => uat-release
- `Staging` => staging
- `Dev` => phase2/dev-tls

## Code Merge
`phase2/dev-tls` --> `staging` --> `uat-release`

## Deployment
- `UAT Deploy` => deploy from client laptop using powershell script
- `Staging Deploy` => github action deploy from staging branch
